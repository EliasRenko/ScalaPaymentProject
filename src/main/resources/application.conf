akka {

    loggers = [akka.event.slf4j.Slf4jLogger]

    loglevel = DEBUG

    logging-filter = "akka.event.slf4j.Slf4jLoggingFilter"

    actor {

        debug {

            unhandled = on
            lifecycle = on
        }
    }
}

scalaPaymentProject {

    balance = 1000

    sourceFile = "src/main/resources/participants.txt"

    mask = """(\w+)\s->\s(\w+)\:\s(\d+)"""
}